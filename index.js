import ZoroPlugin from './src/core/ZoroPlugin.js';

// Export for Obsidian (CommonJS format)
module.exports = {
  default: ZoroPlugin,
};

// Also export for ES6 modules (future-proofing)
export default ZoroPlugin;